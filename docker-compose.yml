services:
  openvpn:
    image: k8scat/openvpn:latest
    cap_add:
      - NET_ADMIN
    ports:
      - "1194:1194/udp"
      - "8833:8833"
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
      - OVPN_GATEWAY=true # 如果是放在网关上用，需要设置为true
    volumes:
      - /srv/openvpn/data:/data
      - /etc/localtime:/etc/localtime:ro
      - /srv/openvpn/logs:/var/log
